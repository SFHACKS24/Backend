# Generate Dockerfile that runs app.py after installation. This service will be hosted on Google Cloud Run.
FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8080

CMD ["python", "app.py"]